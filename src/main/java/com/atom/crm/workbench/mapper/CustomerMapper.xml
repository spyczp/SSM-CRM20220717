<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atom.crm.workbench.mapper.CustomerMapper">
  <resultMap id="BaseResultMap" type="com.atom.crm.workbench.bean.Customer">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Wed Aug 24 10:37:34 CST 2022.
    -->
    <id column="id" jdbcType="CHAR" property="id" />
    <result column="owner" jdbcType="CHAR" property="owner" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="website" jdbcType="VARCHAR" property="website" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="create_by" jdbcType="VARCHAR" property="createBy" />
    <result column="create_time" jdbcType="CHAR" property="createTime" />
    <result column="edit_by" jdbcType="VARCHAR" property="editBy" />
    <result column="edit_time" jdbcType="CHAR" property="editTime" />
    <result column="contact_summary" jdbcType="VARCHAR" property="contactSummary" />
    <result column="next_contact_time" jdbcType="CHAR" property="nextContactTime" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="address" jdbcType="VARCHAR" property="address" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Wed Aug 24 10:37:34 CST 2022.
    -->
    id, owner, name, website, phone, create_by, create_time, edit_by, edit_time, contact_summary, 
    next_contact_time, description, address
  </sql>

  <select id="selectCustomerByName" parameterType="String" resultMap="BaseResultMap">
    select
      id,
      owner,
      name,
      website,
      phone,
      create_by,
      create_time,
      edit_by,
      edit_time,
      contact_summary,
      next_contact_time,
      description,
      address
    from tbl_customer
    where name=#{name}
  </select>

  <select id="selectCustomerNameListByName" parameterType="String" resultType="String">
    select name
    from tbl_customer
    where name like concat('%',#{name},'%')
  </select>

  <select id="selectCustomerByIdConnectOtherTable" parameterType="String" resultMap="BaseResultMap">
    select
      c.id,
      u1.name as owner,
      c.name,
      c.website,
      c.phone,
      u2.name as create_by,
      c.create_time,
      u3.name as edit_by,
      c.edit_time,
      c.contact_summary,
      c.next_contact_time,
      c.description,
      c.address
    from tbl_customer c
    join tbl_user u1 on c.owner=u1.id
    join tbl_user u2 on c.create_by=u2.id
    left join tbl_user u3 on c.edit_by=u3.id
    where c.id=#{id}
  </select>

  <delete id="deleteCustomerByIds" parameterType="String">
    delete from tbl_customer
    where id in
    <foreach collection="array" item="id" separator="," open="(" close=")">
      #{id} /*(id,id,id,id)*/
    </foreach>
  </delete>

  <update id="updateCustomer" parameterType="Customer">
    update tbl_customer
    set
      owner=#{owner},
      name=#{name},
      website=#{website},
      phone=#{phone},
      edit_by=#{editBy},
      edit_time=#{editTime},
      contact_summary=#{contactSummary},
      next_contact_time=#{nextContactTime},
      description=#{description},
      address=#{address}
    where id=#{id}
  </update>

  <select id="selectCustomerById" parameterType="String" resultMap="BaseResultMap">
    select <include refid="Base_Column_List"></include>
    from tbl_customer
    where id=#{id}
  </select>

  <select id="selectCountByCondition" parameterType="map" resultType="int">
    select count(*)
    from tbl_customer c
    join tbl_user u1 on c.owner=u1.id
    <where>
      <if test="name != null and name != ''">
        c.name like concat('%',#{name},'%')
      </if>
      <if test="owner != null and owner != ''">
        and u1.name like concat('%',#{owner},'%')
      </if>
      <if test="phone != null and phone != ''">
        and c.phone like concat('%',#{phone},'%')
      </if>
      <if test="website != null and website != ''">
        and c.website like concat('%',#{website},'%')
      </if>
    </where>
  </select>

  <select id="selectCustomerByCondition" parameterType="map" resultMap="BaseResultMap">
    select
      c.id,
      u1.name as owner,
      c.name,
      c.website,
      c.phone,
      u2.name as create_by,
      c.create_time,
      u3.name as edit_by,
      c.edit_time,
      c.contact_summary,
      c.next_contact_time,
      c.description,
      c.address
    from tbl_customer c
    join tbl_user u1 on c.owner=u1.id
    join tbl_user u2 on c.create_by=u2.id
    left join tbl_user u3 on c.edit_by=u3.id
    <where>
      <if test="name != null and name != ''">
        c.name like concat('%',#{name},'%')
      </if>
      <if test="owner != null and owner != ''">
        and u1.name like concat('%',#{owner},'%')
      </if>
      <if test="phone != null and phone != ''">
        and c.phone like concat('%',#{phone},'%')
      </if>
      <if test="website != null and website != ''">
        and c.website like concat('%',#{website},'%')
      </if>
    </where>
    order by c.create_time desc
    limit #{beginNo},#{pageSize}
  </select>

  <insert id="insertCustomer" parameterType="Customer">
    insert into tbl_customer
    values(
      #{id},
      #{owner},
      #{name},
      #{website},
      #{phone},
      #{createBy},
      #{createTime},
      #{editBy},
      #{editTime},
      #{contactSummary},
      #{nextContactTime},
      #{description},
      #{address}
          )
  </insert>
</mapper>